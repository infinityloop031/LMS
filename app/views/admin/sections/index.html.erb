<h1>Sections</h1>
<div>
  <p>
    <%= link_to "Add Section", new_admin_section_path, class: "btn btn-dark"%>
  </p>
</div>
<table id="enrollment" border="3" layout= "fixed" width=" 100%"  >
<tr>
    <th>Course</th>
    <th>Section</th>
    <th>Teacher</th>
    <th>Semester</th>
    <th>Semester No</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>

  <% @sections.each do |s| %>
    <tr>
        <td><%= s.course.name %></td>
        <td><%= s.name%></td>
        <td><%= s.teacher.name %></td>
        <td><%= s.semester.semester_type %></td>
        <td><%= s.semester.semester_number%></td>
        <td><%= link_to 'Edit', edit_admin_section_path(s), class: "btn btn-dark" %></td>
        <td><%= link_to "Delete", admin_section_path(s), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  }, class: "btn btn-dark" %></td>
      </a>
    </tr>
  <% end %>